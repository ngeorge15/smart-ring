# Environments / dependencies
.venv/
app/node_modules/

# Generated build output
web/dist/
app/dist/
*.tsbuildinfo

# Personal biometric data - never committed
data/
app/src/snapshot.json
web/snapshot.json

# Signed build artifact (regenerated by macOS build step, not source)
SmartRingSync.app/

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Runtime build/ingest locks (serve.py), not source
web/.build.lock
web/.build-serialize.lock
